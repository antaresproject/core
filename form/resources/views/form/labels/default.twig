<label {{ label.getAttributes()|stringify }} for="{{ control.getName() }}">
    {{ label.getName() }}
    {% if label.hasInfo() %}
        <i class="zmdi zmdi-help-outline help--trigger" data-tooltip-inline="{{ label.getInfo() }}"></i>
    {% endif %}
</label>