{% set menu=app('antares.widget').make('menu.control.pane') %}
<ul class="menu-aside">
    {% for item in menu %}
        <li class="{{ item.isChildActive() ? 'is-active' : '' }} {{ (item.childs|length>0 and item.isActive())?'has-submenu submenu-open':''  }}">
            <a href="{{ item.link }}" class="mdl-js-button mdl-js-ripple-effect">
                <i class="zmdi {{ item.icon }}"></i>
                <span>
                    {{ item.title }}
                </span>
            </a>
            {% if item.childs is not empty  %}
                <ul class="menu-aside__submenu">
                    {% for child in item.childs %}
                        <li {{ child.isChildActive()?'class="is-active"':'' }}>
                            <a href="{{ child.link}}" class="mdl-js-button mdl-js-ripple-effect">
                                <i class="zmdi {{ item.icon }}"></i>
                                <span>
                                    {{ child.title}}
                                </span>
                            </a>
                        </li>
                    {% endfor %}
                </ul>
            {% endif %}
        </li>
    {% endfor %}
</ul>